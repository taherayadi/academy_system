-- Migration 0002: suivi_notes unique index + staff CIN unique index.
-- Consolidated into 0001_init.sql (complete schema). Kept as a no-op for
-- migration numbering/ordering compatibility.
SELECT 1;
