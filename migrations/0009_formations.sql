-- Migration 0009: formations & courses module tables.
-- Consolidated into 0001_init.sql (complete schema under the
-- FORMATIONS / COURSES section). Kept as a no-op for migration
-- numbering/ordering compatibility.
SELECT 1;
